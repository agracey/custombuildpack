#!/bin/bash

export BUILDPACK_DIR=`dirname $(readlink -f ${BASH_SOURCE%/*})`
BUILD_DIR=$1
CACHE_DIR=$2
DEPS_DIR=$3/$4

ls $BUILD_DIR
ls $CACHE_DIR
ls $DEPS_DIR

cp $BUILDPACK_DIR/app.js .

$DEPS_DIR/bin/npm npm install 